FROM letcoding-api:local
USER root
RUN apt-get update && apt-get install -y --no-install-recommends docker.io \
    && rm -rf /var/lib/apt/lists/*
USER 10001:10001
CMD ["python3", "manage.py", "worker"]
