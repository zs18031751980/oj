# 代码综合平台

## 介绍
iOS Club 代码综合平台是一个集代码编辑、在线评测、学习资源、统一身份认证和公告管理于一体的综合性平台，旨在为社团成员提供便捷的编程学习与实践环境。

## 项目结构

```
LetCoding/
├── webapi/                  # 后端 API 服务
│   └── fastapi_of_letcoing/ # Flask + Flask-RESTX API 服务
├── webapp/                  # 前端应用
│   └── letapp/              # Vue 3 + TypeScript 实现的单页应用
├── LICENSE                  # 许可证文件
├── 项目详情.md              # 项目功能与目录说明
└── README.md                # 中文 README
```

## 技术栈

### 后端 (webapi/fastapi_of_letcoing)
- **框架**: Flask 3 + Flask-RESTX
- **数据访问**: Peewee ORM + PostgreSQL
- **认证与缓存**: OIDC / OAuth、JWT、Redis
- **主要功能**: 代码执行、在线评测、用户同步、公告管理
- **外部服务**: Glot API (代码执行)

### 前端 (webapp/letapp)
- **框架**: Vue 3 + TypeScript
- **构建工具**: Vite
- **UI 组件**: Naive UI + Tailwind CSS
- **状态管理**: Pinia
- **路由**: Vue Router
- **Markdown 支持**: markdown-it 及其插件

## 功能特点

1. **多语言代码运行**: 支持多种编程语言的在线运行与结果展示
2. **在线评测**: 支持题目浏览、代码提交和测试用例判定
3. **学习资源**: 使用 Markdown 展示学习内容，支持代码高亮、Mermaid 和任务列表
4. **统一身份认证**: 支持 iOSClub OIDC 与 GitHub OAuth，并签发本地 JWT
5. **公告管理**: 公开展示已发布公告，管理身份可新增、修改、删除并管理草稿
6. **Markdown 公告编辑器**: 桌面端左侧编辑原始 Markdown、右侧实时渲染，移动端上下排列
7. **主题切换**: 支持明暗主题切换，并持久化用户偏好
8. **响应式设计**: 适配桌面与移动端屏幕
9. **模块化架构**: 前后端分离，控制器、服务、模型和页面职责明确

## 身份与权限同步

系统在第三方身份登录成功后，通过 `provider + provider_id` 查找本地用户并原地更新，已有用户主键保持不变。同步内容包括用户名、邮箱、头像、可识别的账号状态和 `last_login`。

内部权限按 `manager > staff > member` 排序。第三方身份只会提高本地角色，不会降低或覆盖本地手工提升的更高权限；账号状态仅在提供方明确返回可识别状态时更新。同步后的 `is_active` 与 `last_login` 会进入 JWT 和 Redis 用户缓存，停用账号不会签发本地令牌。

| 内部角色 | 对应身份 | 公告权限 |
|---------|---------|---------|
| `manager` | 管理员、社长、副社长、部长、副部长、部门主管 | 查看草稿及新增、修改、删除公告 |
| `staff` | 部员、干事 | 查看已发布公告 |
| `member` | 社员、普通用户 | 查看已发布公告 |

## 公告管理流程

- `GET /announcement/` 是公开接口，仅返回已发布公告。
- `GET /announcement/?include_unpublished=true` 返回已发布公告与草稿，仅限 `manager`。
- `POST /announcement/`、`PUT /announcement/<id>`、`DELETE /announcement/<id>` 仅限 `manager`。
- 草稿详情对匿名用户和普通成员返回不存在，避免草稿内容泄露。
- 修改操作直接保存原公告记录，不删除重建，因此公告主键保持不变。
- 前端 `/admin` 路由进行管理身份守卫，后端鉴权作为最终权限边界。
- 保存或删除失败时保留编辑内容和页面状态，删除前使用确认对话框。

## 快速开始

### 后端服务

1. 进入后端目录
   ```bash
   cd webapi/fastapi_of_letcoing
   ```

2. 安装依赖
   ```bash
   pip install -r requirements.txt
   ```

3. 配置环境变量
   ```bash
   export API_TOKEN=your_glot_api_token
   ```

4. 启动服务
   ```bash
   python main.py
   ```

### 前端应用

1. 进入前端目录
   ```bash
   cd webapp/letapp
   ```

2. 安装依赖
   ```bash
   npm install
   ```

3. 启动开发服务器
   ```bash
   npm run dev
   ```

4. 构建生产版本
   ```bash
   npm run build
   ```

5. 访问应用
   打开浏览器访问 `http://localhost:5173`

## 目录说明

### webapi/fastapi_of_letcoing
- `main.py`: Flask 应用入口与命名空间注册
- `controllers/auth_controller.py`: OAuth / OIDC 登录、身份同步与令牌签发
- `controllers/announcement_controller.py`: 公告公开读取、管理读取与 CRUD 接口
- `services/user_service.py`: 用户原地同步、角色提升和最后登录时间更新
- `services/jwt_service.py`: JWT 签发、校验与 Redis 用户缓存
- `models/db_models.py`: 用户、题目、提交、用户代码和公告 ORM 模型
- `services/glot_service.py`: 调用 Glot API 执行代码的服务
- `requirements.txt`: 项目依赖列表

### webapp/letapp
- `src/`: 源代码目录
  - `components/`: Vue 组件
  - `layouts/`: 布局组件
  - `pages/`: 公开页面与公告管理页面
  - `stores/`: Pinia 状态管理
  - `services/`: REST API 请求与数据类型
  - `utils/`: 公告权限、查询参数和表单纯函数
  - `types/`: TypeScript 类型定义
  - `App.vue`: 根组件
  - `main.ts`: 应用入口
  - `router.ts`: 路由配置
- `public/`: 静态资源
- `index.html`: HTML 模板

## 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。
